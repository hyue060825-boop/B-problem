# 接收记录：3ddb2d9

main 接收 hy 截至 `3ddb2d9e914de17f1d2cce8d8f7cf9f4bd423e45` 的资产，包含此前未接收的 `ffea626`（Q4 诊断）和 `0720358`（同步 DDP）。保留 main 的题面、论文和历史结果；本次仅适配目录、入口及源码校验，不另外改动 hy 的算法。

## 更新范围

- Q4：在同一频道完成全部认证站点的无信号观测后允许认证不存在；校验站点坐标，移除重复重扫，修复清完后无法退出的问题。
- 训练：同步 DDP 的批次数、有效样本权重、PPO 优势统计及参数/Adam 状态；新增 Q3 搜索教师、GPU 搜索、联合微调和 Q4 按时间预算训练。
- 筛选与部署：收紧教师完成率和配对选模条件，统一传递宏动作预算；部署实时预算递减，权重加载检查关键源码。
- 资产：接收 373 个新增或变化路径，其中原始记录、报告及配置 339 份、权重 10 份；[映射表](../../results/validation/integration-3ddb2d9/asset-map.csv)记录来源与内容校验值。

源码仍在 `src/solution/`，新增 `search/`；测试在 `tests/solution/`。交付原件集中保存在 [import-3ddb2d9](../../results/training/import-3ddb2d9/README.md)，活动配置在 `experiments/q3/`、`experiments/q4/`，新运行使用独立的 `results/` 输出目录。源码校验兼容旧的 `solution/`、`bsim/` 键及 main 的 `src/` 键，均核验实际文件哈希；未修改权重内部 provenance。

## 结果与验证

最新配对记录共 12000 局，四组各 3000 局均标记完成。已复核种子唯一性、同场景配对、四份权重哈希和均值：Q3 候选相对冻结原模型平均每局少 14.32 秒，Q4 五小时训练模型相对训练前模型少 308.35 秒。[报告与原始数据](../../results/training/import-3ddb2d9/runs/q34_3000_test_20260912/report.md)均属于自建研究分布。

本机回归 184 项测试、69 项子测试通过，2 项 CUDA 测试跳过；另新增 1 项目录与权重校验测试通过。四份权重各运行 2 局，共 8 局全部完成；Q3/Q4 各一次真实 HTTP 部署分别清除 10/10、13/13 个源并退出，实时预算单调递减。详见[本次验证](../../results/validation/integration-3ddb2d9/README.md)。未在本机重训、重跑完整 12000 局或运行官方模拟器。

## 使用边界

- Q3 新权重仍称候选：此前稳健验证门槛推荐冻结原模型；最新测试支持小幅平均改善，不说明每个场景都更快，也不直接替代原选模规则。
- 旧 Q4 失败记录保持原样；修复后结果不能回写成旧控制器或旧训练已经达标。中途报告里的“待补充”属于当时快照，最新数值从上述配对报告取用。
- 完整搜索标签、部分训练中间权重仍在训练机，重新训练需要先生成标签。`q4_preflight.py` 的旧权重诊断依赖未交付的 `ddp_20260911/q4/latest.pt`，不能直接完整复跑；已交付的预检记录可供核对。
- `evaluate_3000.py` 现支持 `--problem 3/4`，但仍仅检查特征版本；优先使用严格加载权重的 `evaluate_latest_pair.py` 或 `evaluate_checkpoint.py`。完整 HTTP 请求/响应持久化及异常退出处理仍待完善。
- 当前 CPU 环境未复验真实 CUDA 搜索或多卡训练吞吐。Q1/Q2 的既有说明差异与夹具问题仍见[实现索引](../../docs/model/实现索引.md)。官方兼容性和正式成绩需单独验证。
