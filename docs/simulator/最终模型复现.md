# 最终模型复现

固定来源 `experiment_result@8ef9ef8`，模型身份见[清单](../../records/inventory/models-final-20260913.json)。原始实验与原配置保持不变；跨机器复现使用新的输出目录。

## 已有数据复算

```bash
python scripts/build_final_handoff_assets.py
python scripts/verify_final_assets.py
python scripts/verify_final_assets.py --deep-archives --output .local/verification-deep.json
python scripts/verify_q4_certificate_exact.py results/final-20260913/q4_optimization_20260912/layout31_proof.json
```

复算更新 `handoff/tables/final-20260913/` 与模型、输入清单，不写 `paper/`，也不改原始图表。主结果区间为配对均值 ±1.96 SE；Q4 规划对照原报告的 bootstrap 区间保留原口径。训练量区分入选 best 之前与整段运行，父权重此前训练另计。

核验入口检查模型和冻结源码、1475 项来源映射、输入哈希、分卷、主均值及当前说明链接；`--deep-archives` 进一步流式读取全部成员。缺少服务器 cache 的旧权重不冒充已备份。

## 新场景配对评测

```bash
python scripts/evaluate_final_pair.py --output results/rehearsal/final-pair-new --episodes 3000 --workers 16 --q3-seed 1930000000 --q4-seed 1940000000
```

种子必须显式指定，重复实验另选未使用区间。Q3 对照 `8cc3a162`，Q4 对照 `e8a525f7`；候选分别为 `51397bc8`、`c8812ced`。入口加载冻结代码并保留源码校验，输出目录必须不存在。

场景为自建研究分布，保留实时剩余现实时间特征，硬件负载可影响动作，不能保证每局结果逐位相同。原 Q4 主评测在 rank 内先父模型后候选；新入口交替顺序，引用新结果时注明区别。`evaluate_deadline_pair.py` 保留旧阶段的模型组合，其中 Q4 候选为当前父模型，不能当作最终 Q4 评测。

## 训练方案复现

原配置在 [Q3](../../results/final-20260913/q3_legacy_deadline_20260913/config.json) 和 [Q4](../../results/final-20260913/q4_baseline_8gpu_1h_20260913/train/config.json)。另存副本，修改父权重、本机输出路径和未来的 `train_deadline_epoch`，不要改归档配置。Q3 父模型取 `results/final-20260913/q3_gpu8_extended_20260912/ppo/best.pt`；Q4 取 `results/final-20260913/q4_legacy_deadline_20260913/best.pt`。

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 python -m torch.distributed.run --standalone --nproc_per_node=4 scripts/train_legacy_deadline_ddp.py --config NEW_Q3_CONFIG.json
```

Q4 使用 8 个 rank 和对应原配置。`launch_q4_baseline_8gpu_hour.py --output NEW_DIRECTORY` 保留八卡一小时、smoke、恢复检查和自动配对流程；其中历史种子按原方案重复，若开展新研究需先更新配置。训练保持父模型、优化器及多卡同步校验。本次整合未重跑 GPU 长训。

## 历史与补充实验

```bash
python scripts/extract_experiment_archive.py results/history/experiment_result-8ef9ef8/runs/q3_gpu8_extended_20260912/archive_manifest.json .local/q3-gpu8-evidence
```

解包目标必须是新目录，原路径在目标内保留。旧文档与原始 provenance 保留当时路径，查阅从[实验索引](../../handoff/shared/最终实验索引.md)或[映射表](../../records/acceptance/experiment_result-8ef9ef8/资产落位建议.csv)进入。

Q4 尾段、空场景、规划对照和极端布局入口使用父模型；Q3 极端布局使用最终模型。旧 `report_*` 脚本会在输入目录输出衍生文件，先复制所需实验到 `.local/` 等新工作目录，再传 `--input`；入口拒绝在归档批次内改写。`report_q4_hour_pair.py`、`plot_q4_last_clear_comparison.py` 单独通过 `--output` 指定新报告目录。几何与训练算法保持交付版本。
