# 系统集成与部署报告

日期：2026-09-11。既有LOCAL HTTP、参考/加速内核、网页和客户端回归：PASS；新增Q1/Q2 CLI与Q3/Q4组件功能检查：PASS；在线Q3/Q4完整策略运行：PARTIAL；官方运行：NOT_RUN。

## 入口

几何入口：

```bash
.venv/bin/python -m solution.cli solve-q1 --input fixtures/q1_triangle.json --output artifacts/q1/triangle
.venv/bin/python -m solution.cli solve-q2 --input fixtures/q2_example.json --output artifacts/q2/example
```

既有模拟器验证入口：

```bash
.venv/bin/python -m bsim validate-fixtures --profile fixture_conformance
.venv/bin/python -m bsim replay --fixture fixtures/timing.json --trace fixtures/timing.trace.json
.venv/bin/python -m pytest -q
```

本地HTTP服务默认需显式夹具和非官方端口：

```bash
.venv/bin/python -m bsim serve --profile fixture_conformance --fixture fixtures/visual_mixed.json --host 127.0.0.1 --port 20260
```

训练电脑和测试电脑职责分离：训练代码产生导出权重，测试程序加载自身权重后逐条调用四个官方接口；不能上传模型给模拟器，也不能从管理网页端口读取真值。默认未执行官方地址 `127.0.0.1:2026`。

## 依赖与证据

Python 3.11、numpy、scipy、Shapely、matplotlib、requests；RL功能检查使用CPU PyTorch。依赖声明见 [pyproject.toml](../pyproject.toml)，本地环境为 `.venv`。源文件哈希见 [source_manifest.json](../rules/source_manifest.json)，官方规则矩阵和G01-G09缺口见仓库既有 `docs/rules_matrix.md`、`docs/official_gaps.md`。

本轮完整回归为49项测试、15个参数化子测试，日志见 [all_tests.txt](raw/all_tests.txt)。Q1、Q2图表和JSON均保存在 `artifacts/q1`、`artifacts/q2`；Q3连续覆盖和Q4网格证书在 `artifacts/q3`、`artifacts/q4`。

官方登录、演练、正式测试、加密日志导出和上传均未执行；官方隐藏分布、固定误差场和精确量化仍阻断逐输出一致性。任何正式运行前必须由用户在官方软件中确认模块、次数和截止时间，并冻结规则、候选器、模型和参数。
