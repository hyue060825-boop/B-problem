# GitHub 协作速查

适用于三人、三天竞赛。两人负责建模与实验，论文手负责 `paper/`，通过 `handoff/` 同步写作材料。命令行、编辑器或 GitHub Desktop 均可。

## 开工与同步

开始任务时在队内说清负责内容；涉及共享接口、同一文件时先协调。尽早同步可用进展，避免比赛最后才合并。

首次初始化由一人完成初始提交并推送 `main`，其他队员再拉取。以下为首次提交后的日常流程；切换分支前先提交当前工作，或暂存并确认可恢复。

```bash
git status
git switch main
git pull --ff-only
git switch -c localization-q1  # 按任务起简短名称，不照抄已存在的分支
```

新算法、接口改动或可能影响运行的实验修改使用短期任务分支；`main` 保持已整合的可用版本。多个 Codex 会话同时工作时，使用独立克隆或 worktree 隔离文件。

## 提交与合并

```bash
git diff
git add -- 本次修改的文件路径
git diff --cached
git commit -m "feat: 完成交会定位算法"
git push -u origin HEAD
```

每次提交一个相对完整的改动，说明可用中文。常用前缀：`feat` 新功能、`fix` 修正、`docs` 文档、`chore` 环境与整理；按需使用即可。

在 GitHub 发起 PR，简述“改了什么、如何验证、还有什么问题”，请另一人快速看过后合并。涉及算法的改动给出一次可复现的验证结果。合并后及时回到 `main` 拉取更新，再开始下一项工作。

少量文档修订或论文手独立维护的文件，可在同步后直接提交到 `main`；影响模型结论或共用代码的修改仍走任务分支。不要为了整理提交历史反复重写共享分支。

## 冲突与交付

- 禁止强推 `main`，不要用丢弃修改的命令覆盖队友工作。推送被拒绝时，先保存本地工作并同步远端变更。
- 分支落后时，先在干净工作区运行 `git fetch origin`，再在自己的任务分支运行 `git merge origin/main`；有冲突时与相关队员核对，保留双方需要的内容，解决后重新验证并提交。
- Word 等二进制论文文件由论文手统一编辑；其余两人修改 `handoff/` 材料，避免同时编辑同一份正文。
- 向论文手交付时更新 `handoff/README.md`，注明材料状态、对应实验和图表位置；新结果替代旧结论时写清变化。

参考 AgenticFinLab 的 GitHub 指南，已按本队规模简化。
